/**
 * Intentionally empty. This package reserves the seam for a future AI
 * assistant (assistant/tools/embeddings/rag/prompts) that will read from the
 * domain services in apps/api — no LLM/vector-store dependencies belong here
 * until that phase begins.
 */
export {};
